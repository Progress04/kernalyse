[project]
name = "kernalyse"
version = "0.1.0"
description = "CUDA Kernel Profiler and Visualiser"
dependencies = [
  "pandas",
  "click",
  "sqlite-utils",
  "plotly",
  "fastapi",
  "uvicorn",
  "pyparsing"
]
